[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python parser.py /app/Informes_X"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
